{
    "name": "KickerCamera",
    "description": "This is a deployment of a KickerCamera",
    "repository": "https://github.com/ijnek/kicker-camera",
    "keywords": ["line"],
    "env": {
        "LINE_BOT_CHANNEL_TOKEN": {
            "description": "LINE bot Channel access token from the Channel Console",
            "required":  true
        },
        "LINE_BOT_CHANNEL_SECRET": {
            "description": "LINE bot Channel secret from the Channel Console",
            "required":  true
        }
    }
}